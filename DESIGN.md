# DESIGN.md

LocalNetFTP 的 UI 以轻量、稳定、少打扰为核心。本文固定高频设计规则，避免后续界面风格分散。

## 基础风格

- UI 框架：PySide6 / Qt for Python。
- 字体：优先 `"Microsoft YaHei UI", "Segoe UI", sans-serif`。
- 颜色：浅色、低对比背景，主操作色使用 `#2f7dd1`。
- 圆角：窗口内控件以 5-8px 为主；不要使用夸张大圆角。
- 间距：普通窗口外边距 14px，toast 外边距 12px，控件间距 8-10px。
- 保存设置、显示二维码、开始接收等主操作使用蓝底白字；禁用态、键盘焦点必须可辨识。
- 设置页使用标题、说明、输入区和页内保存反馈；空名称、空目录或非完整目录路径在原页提示并聚焦输入项。
- 悬浮窗显示接收方选择数量及 Ctrl 多选提示；设备名称过长使用省略号，状态文字换行。
- 二维码接收的网卡选择与生成按钮同排；IP 放在网卡名称前，完整名称通过悬停提示查看，保存路径可选中复制。
- ticket 输入为空时禁用开始接收按钮。

## 窗口类型

- 主悬浮窗：小尺寸、半透明、置顶，只放用户列表、名称、粘贴/输入栏和进度。
- 主悬浮窗使用项目内置关闭按钮，按钮右侧必须保留可见 margin，不使用贴边的系统标题栏红叉。
- 设置/投送/ticket 窗口：普通 Qt 窗口，使用统一标题、说明、输入区、状态行和右对齐按钮。
- 发送/接收通知：toast 形态，右下角出现，可拖动，可关闭。

## Toast 规则

- 初始位置固定在 Windows 工作区右下角，底边贴齐任务栏顶边，不与任务栏重叠。
- toast 内容变化、图片/文本预览插入、进度文字变化后必须重新定位。
- 接收 toast 不自动消失；发送 toast 成功后 5 秒关闭，失败/取消时保留重试入口。
- toast 必须有右上角 `×` 关闭按钮。

## Ticket 窗口

- 展示 ticket 和输入 ticket 不能使用系统默认输入弹窗，必须使用项目自定义窗口。
- 点击“复制 ticket”后在窗口状态行显示“已复制 ticket”，不要弹出 popup。
- 输入 ticket 窗口使用标题、说明、文本框、状态行、按钮区的统一结构。

## 手机网页下载

- “局域网内手机用户（网页）”是主悬浮窗里的固定发送目标，必须排在“局域网外用户（生成 ticket）”前面。
- 拖动文件/文件夹给该目标后，生成只在窗口存在期间有效的局域网网页下载服务。
- 下载网页必须直接读取用户拖入的硬盘原文件路径，不复制到临时目录。
- 手机网页下载与二维码接收共用网卡选择和二维码组件：先选网卡，再显示一个二维码，配完整网址及复制按钮。
- 点击网址只在窗口状态行显示复制成功，不弹出 popup。

## 从手机接收

- 从托盘右键菜单进入“从二维码接收文件”，窗口样式与手机网页下载保持一致。
- 接收窗口先选择网卡名称/IP，点击“显示二维码”后仅显示所选网卡的网址和二维码；切换网卡隐藏旧二维码。关闭窗口停止服务。
- 两类窗口共享并持久保存网卡名称和地址，已打开的窗口同步选择；IP 变化优先匹配同名网卡，不可用时回退至当前可用网卡。
- 二维码居中展示，按实际像素尺寸保留空间，展示区域不使用滚动条；生成后窗口按内容调整大小，保证完整可见。
- 网页支持单个/多个文件、文字和粘贴图片；图片可预览和移除，点击发送后保存到电脑。浏览器不支持图片粘贴时通过文件选择器选择照片。
- 上传期间禁用编辑及重复提交；失败保留输入以便重试，成功显示保存数量并清空表单。
- 粘贴文本以 UTF-8 文件上传并保存为文字文件，支持超出普通表单字段限制的长日志；服务端错误显示明确原因，非 JSON 响应不得导致误报解析错误或清空输入。
- 手机上传内容直接保存到电脑当前接收目录，文件名冲突必须自动避让，不覆盖已有文件。
- 手机上传完成后使用与局域网 PC 来文件一致的接收 toast、预览和打开位置逻辑。
- 点击网址只在窗口状态行显示复制成功，不弹出 popup。

## 文案

- 接收提示的“打开保存位置”直接打开实际收到项目的父目录，不通过 Explorer 参数选择文件；打开失败保留提示并显示原因。
- 本地和手机网页文本预览只读取有限前缀，截断时保留完整字符并显示省略号，避免大文件阻塞界面。

- 文案短、直接、偏操作状态。
- 错误可显示具体原因，但避免长段技术栈信息直接占满 UI。
- 状态反馈优先显示在当前窗口的状态行，少用阻塞式 popup。
- 复制类操作必须在原窗口内给出短反馈，例如按钮文字变为“已复制”或状态行显示“已复制 ticket”。
