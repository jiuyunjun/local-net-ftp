# DESIGN.md

LocalNetFTP 的 UI 以轻量、稳定、少打扰为核心。本文固定高频设计规则，避免后续界面风格分散。

## 基础风格

- UI 框架：PySide6 / Qt for Python。
- 字体：优先 `"Microsoft YaHei UI", "Segoe UI", sans-serif`。
- 颜色：浅色、低对比背景，主操作色使用 `#2f7dd1`。
- 圆角：窗口内控件以 5-8px 为主；不要使用夸张大圆角。
- 间距：普通窗口外边距 14px，toast 外边距 12px，控件间距 8-10px。

## 窗口类型

- 主悬浮窗：小尺寸、半透明、置顶，只放用户列表、名称、粘贴/输入栏和进度。
- 设置/投送/ticket 窗口：普通 Qt 窗口，使用统一标题、说明、输入区、状态行和右对齐按钮。
- 发送/接收通知：toast 形态，右下角出现，可拖动，可关闭。

## Toast 规则

- 初始位置固定在 Windows 工作区右下角，底边贴齐任务栏顶边，不与任务栏重叠。
- toast 内容变化、图片/文本预览插入、进度文字变化后必须重新定位。
- 接收 toast 不自动消失；发送 toast 成功后 5 秒关闭，失败/取消时保留重试入口。
- toast 必须有右上角 `×` 关闭按钮。

## Ticket 窗口

- 展示 ticket 和输入 ticket 不能使用系统默认输入弹窗，必须使用项目自定义窗口。
- 点击“复制 ticket”后在窗口状态行显示“已复制 ticket”，不要弹出 popup。
- 输入 ticket 窗口使用标题、说明、文本框、状态行、按钮区的统一结构。

## 文案

- 文案短、直接、偏操作状态。
- 错误可显示具体原因，但避免长段技术栈信息直接占满 UI。
- 状态反馈优先显示在当前窗口的状态行，少用阻塞式 popup。
